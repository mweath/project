this is a test file
a new file

adding another line

> usethis::use_git_config(user.name = "mweath", user.email = "mweath@uw.edu")
> usethis::use_git()

Selection: 1

additional texts


so that we don't have to keep punching in username and password

usethis::browse_github_pat()

● Call `usethis::edit_r_environ()` to open '.Renviron'.
● Store your PAT with a line like:
  GITHUB_PAT=xxxyyyzzz
● Make sure '.Renviron' ends with a newline!
